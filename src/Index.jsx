import React from 'react';
import { render } from 'react-dom';
// import PropTypes from 'prop-types';

const App = () => {
	return (
		<p>Beginnings of a React app.</p>
	)
};

render(
	<App />,
	document.getElementById('root')
);